内存里所有数据都是unicode编码
read(n) 读n个字节/字符（跟用rb/r打开有关，常用于读取视频文件，过大不好，需要逐步读取）

lst，dic快捷传参
    def cal(a,b,c):
    lst = [1,2,3]
    dic = {'a':1,'b':2,'c':3}
    cal(*lst)
    cal(**dic)

参数读陷阱问题


函数读名字是可以赋值的，类似变量，函数名可以作为另一个函数的参数传入，也可以做返回值，重点是其指向的函数主体所在的内存空间

闭包函数：内部函数引用了外部函数的变量，那么这个内部函数就是一个闭包函数

生成器